<Page x:Class="Diplom.Pages.Profile"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Diplom.Pages"
      mc:Ignorable="d" 
     d:DesignHeight="800" d:DesignWidth="1500"
      Title="Profile">
    <Page.Resources>
        <Style TargetType="TextBlock" >
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
            <Button x:Name="BtnPhoto" Content="Изменить фотографию" Click="ChangePhoto_Click"/>
            <Button x:Name="BtnData" Content="Обновить данные" Click="ChangeData_Click"/>
            
        </StackPanel>
        <StackPanel Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image x:Name="ImgUser" Margin="50" Height =" 300" Width="300"/>
            <TextBlock x:Name="TxtLogin"/>
            <TextBlock x:Name="TxtName"/>
            <TextBlock x:Name="TxtPatronymic"/>
            <TextBlock x:Name="TxtLastName"/>
        </StackPanel>
    </Grid>
</Page>
